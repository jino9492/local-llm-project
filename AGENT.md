# Ollama 기반 AI 비속어 검열기 개발 가이드

이 문서는 4인 팀이 1시간 동안 시연용 프로그램을 개발하기 위한 역할 분담, 작업 순서, 연동 규격을 정의한다. 아래 내용은 개발 계획이며, 구현 완료나 모델 성능 검증 결과를 의미하지 않는다.

## 1. 프로젝트 목표

사용자가 입력한 한국어 문장을 로컬 LLM으로 분석하여, 띄어쓰기나 부분 문자열 검색 때문에 발생하는 비속어 오탐을 줄일 수 있는지 확인한다.

동일한 입력에 대해 다음 두 결과를 나란히 보여준다.

- **단순 금칙어 필터:** 공백을 제거한 문자열에서 금칙어 포함 여부를 검사한다.
- **AI 문맥 판단:** 공백을 보존한 원문 전체를 Ollama의 로컬 모델로 분석한다.

단순 필터는 비교 기준으로 사용한다. 이번 버전에서는 단순 필터의 검출 여부와 관계없이 모든 입력 문장을 LLM으로 검사한다.

## 2. 개발 범위

| 항목 | 구현 범위 |
|---|---|
| 개발 인원 | PM·프론트·백엔드·기타(AI) 각 1명 |
| 개발 시간 | 60분 |
| 모델 실행 | Ollama를 사용하는 로컬 LLM |
| 입력 | 최대 300자의 한국어 문장 |
| 화면 | 입력창, 검사 버튼, 예시 문장 버튼, 결과 비교 영역 |
| 결과 | 허용 / 차단 / 확인 필요, 판단 이유, 처리 시간 |
| 검열 처리 | 허용 시 원문, 차단 시 안내 문구, 확인 필요 시 검토 안내 |
| 이후 확장 | 로그인, DB, 채팅 서비스 연결, 모델 학습, 단어별 마스킹 |

가능하면 개발 시작 전에 Ollama 설치와 모델 다운로드를 완료한다. 준비되지 않았다면 AI 담당이 개발 시작과 동시에 진행한다. 설치·다운로드 시간과 장비의 추론 속도는 60분 내 완성에 영향을 줄 수 있다.

## 3. 공통 판단 기준

| 상황 | 판정 |
|---|---|
| 일반적인 정상 문장 | `ALLOW` — 허용 |
| 정상 단어 내부에 우연히 금칙어가 포함됨 | `ALLOW` — 허용 |
| 공백 제거 과정에서 정상 표현이 금칙어처럼 보임 | `ALLOW` — 허용 |
| 상대를 욕하거나 감탄사로 실제 욕설을 사용함 | `BLOCK` — 차단 |
| 욕설 사이에 공백을 넣어 우회함 | `BLOCK` — 차단 |
| 문맥이 부족하여 판단하기 어려움 | `REVIEW` — 확인 필요 |
| 설명·인용·신고 문맥에 욕설이 포함됨 | 시작 5분 안에 PM이 팀과 허용 기준을 확정 |

- 원문을 보존하고, 공백을 제거한 문자열만으로 AI가 최종 판단하게 하지 않는다.
- `REVIEW`는 문맥상 판단 보류를 의미한다. 시스템 오류를 나타내는 값이 아니다.
- Ollama 연결 실패, 시간 초과, 잘못된 모델 출력은 별도의 검사 실패로 처리한다.
- 검사 실패를 `ALLOW` 또는 `REVIEW`로 바꾸지 않는다.

## 4. 역할별 작업

### 4.1 PM — 기획·일정·최종 테스트

**책임:** 기능 범위와 판정 기준을 확정하고, 제한 시간 안에 연결된 결과물이 완성되도록 관리한다.

1. **기능 범위 확정**
   - 문장 입력, 검사, 단순 필터와 AI 결과 비교, 판단 이유 표시를 필수 기능으로 정한다.
   - 1시간 내 필수 기능 완성을 우선하고 이후 확장 기능은 별도로 기록한다.
2. **검열 기준 확정**
   - 허용·차단·확인 필요 기준을 AI 담당과 공유한다.
   - 설명·인용·신고 속 욕설을 허용하는 조건을 결정한다.
3. **테스트 문장 20개 준비**
   - 일반 정상 문장 5개.
   - 띄어쓰기·부분 문자열로 오탐하기 쉬운 정상 문장 5개.
   - 명확한 욕설 5개.
   - 공백을 삽입한 우회 욕설 3개.
   - 문맥이 부족한 표현 2개.
   - 각 문장에 기대 판정과 이유를 기록한다.
   - 프롬프트 예시로 사용하지 않은 문장도 포함한다.
4. **진행 관리**
   - 20분까지 실제 모델 호출 성공 여부를 확인한다.
   - 35분까지 화면에서 실제 AI 판정이 표시되는지 확인한다.
   - 45분 이후에는 새 기능 추가를 멈추고 오류 수정에 집중한다.
5. **최종 검증과 시연 준비**
   - 정상 문장을 잘못 차단한 수, 욕설을 놓친 수, 확인 필요 건수, 응답 시간을 기록한다.
   - 문제 사례 → 단순 필터 결과 → AI 판단 결과 순으로 시연한다.
   - 작은 테스트 결과를 전체 한국어 문장에 대한 성능으로 일반화하지 않는다.

**산출물:** 기능·판정 기준표, 테스트 문장과 결과표, 시연 순서.

### 4.2 프론트 — 입력·비교 결과 화면

**책임:** 사용자가 문장을 입력하고 검사 결과를 이해할 수 있는 단일 페이지를 완성한다.

1. **입력 화면 구현**
   - 최대 300자 입력창과 글자 수 표시.
   - 검사 버튼.
   - 정상 문장·오탐 사례·욕설 사례를 입력하는 예시 버튼 3개.
   - 빈 입력 안내.
2. **결과 비교 화면 구현**
   - 단순 필터: 금칙어 검출 / 미검출.
   - AI 판단: 허용 / 차단 / 확인 필요.
   - AI 판단 이유와 실제 처리 시간.
   - 색상과 함께 판정 문구를 표시한다.
3. **검열 결과 표시**
   - 허용: 원문 표시.
   - 차단: 차단 안내 문구 표시.
   - 확인 필요: 검토 안내 표시.
4. **API 연결**
   - 합의한 예시 JSON으로 화면을 먼저 구현한다.
   - 백엔드 준비 후 `POST /api/check`로 연결한다.
   - 임시 응답을 사용한 화면 확인과 실제 모델 연동 확인을 구분한다.
5. **대기·실패 상황 처리**
   - 검사 중 로딩 표시와 중복 클릭 방지.
   - 검사 실패 안내.
   - 이전 판정이 새 요청의 결과처럼 남지 않도록 처리.

**산출물:** 실제 API에 연결된 입력·결과 비교·로딩·오류 처리 페이지.

### 4.3 백엔드 — 검사 API·전체 처리 흐름

**책임:** 입력을 검증하고 단순 필터와 AI 모듈을 실행한 뒤 일정한 형식으로 결과를 반환한다.

1. **검사 API 구현**
   - `POST /api/check`에서 `text`를 받는다.
   - 문자열 여부, 빈 입력 여부, 최대 길이를 검사한다.
2. **비교용 단순 필터 구현**
   - 시연에 필요한 소수의 금칙어로 시작한다.
   - 비교용 문자열에서 공백을 제거한 후 포함 여부를 검사한다.
   - 원문은 별도로 보존한다.
   - 단순 필터가 AI의 최종 판정을 덮어쓰지 않도록 한다.
3. **AI 모듈 연결**
   - AI 담당이 만든 `classify(text)`에 원문을 전달한다.
   - 반환된 `decision`과 `reason`을 받는다.
   - 프론트는 백엔드만 호출하도록 구성한다.
4. **응답 검증과 시간 측정**
   - 판정값이 `ALLOW`, `BLOCK`, `REVIEW` 중 하나인지 확인한다.
   - 필요한 필드가 존재하고 형식이 올바른지 확인한다.
   - 요청 처리 시간을 측정한다.
   - 단순 필터 결과와 처리 시간을 합쳐 프론트에 반환한다.
5. **오류 처리**
   - 입력 오류, Ollama 미실행, 응답 시간 초과, 잘못된 모델 출력을 구분한다.
   - 적절한 오류 응답을 반환하고 정상 판정으로 처리하지 않는다.
6. **실행 방법 정리**
   - 필요한 패키지와 서버 실행 명령을 기록한다.
   - 최종 재실행과 전체 연결을 확인한다.

**산출물:** 입력 검증·단순 필터·AI 연동·응답 검증·오류 처리가 가능한 검사 API와 실행 방법.

### 4.4 기타(AI) — Ollama·모델·프롬프트

**책임:** 로컬 모델을 실행하고 문맥 분류 함수를 만들어 백엔드에 전달한다.

1. **Ollama 준비와 모델 확정**
   - 로컬 모델을 다운로드하고 실행한다.
   - 장비에서 실행 가능한 한국어 처리 모델 하나를 우선 확보한다.
   - 정상 문장과 욕설 문장으로 실제 호출을 확인한다.
   - 첫 호출로 모델을 미리 로딩한 뒤 응답 속도를 확인한다.
2. **분류 프롬프트 작성**
   - 공백을 보존한 원문 전체의 의미를 고려하도록 한다.
   - 정상 단어에 우연히 포함된 금칙어와 실제 욕설을 구분하도록 한다.
   - 공백을 삽입한 우회 욕설을 판단하도록 한다.
   - 설명·인용 문맥에는 PM이 확정한 기준을 적용한다.
   - 판단하기 어려우면 `REVIEW`를 반환하도록 한다.
   - 입력문 안의 명령도 분류할 데이터로 취급하도록 한다.
3. **출력 형식 고정**
   - 모델 출력은 `decision`과 `reason`으로 제한한다.
   - `reason`은 짧은 한국어 한 문장으로 작성하도록 한다.
   - Ollama의 `format`에 JSON Schema를 설정한다.
   - `stream: false`를 사용하고 낮은 temperature로 시작한다.
   - 구조화 출력의 형식 준수와 판단 정확도를 별도로 확인한다.
4. **호출 함수 전달**
   - `classify(text)` 안에서 Ollama를 호출하고 판정·이유를 반환한다.
   - 연결·출력 오류는 백엔드가 검사 실패로 처리할 수 있게 전달한다.
   - 모델 이름, 연결 주소, 필요한 패키지, 실행 방법을 함께 전달한다.
   - 20분까지 실제 모델 응답이 나오는 것을 목표로 한다.
5. **판단 품질 개선**
   - PM이 기록한 오탐·미탐 사례를 확인한다.
   - 실패 유형에 맞춰 프롬프트를 수정한다.
   - 수정 후 정상 문장과 욕설 사례를 모두 다시 검사한다.

**산출물:** 실행되는 로컬 모델, 분류 프롬프트, `classify(text)` 함수, 실패 사례와 수정 기록.

## 5. 60분 개발 일정

| 시간 | PM | 프론트 | 백엔드 | 기타(AI) |
|---|---|---|---|---|
| 0~5분 | 목표·판정 기준 확정 | 화면 구성 합의 | API 형식 합의 | 모델 준비 시작 |
| 5~20분 | 테스트 문장·기대 결과 작성 | 입력·결과 화면 구현 | API·단순 필터 구현 | 모델 호출·프롬프트·함수 구현 |
| 20~35분 | 연결 상태 확인 | 실제 API 연결 | AI 모듈 연결·통합 오류 수정 | 호출 함수 전달·판정 점검 |
| 35~45분 | 테스트 실행·결과 기록 | 대기·오류 화면 수정 | 입력·실패 처리 점검 | 오탐·미탐 개선 |
| 45~55분 | 결과·발표 정리 | 시연 화면 정리 | 실행 방법 정리 | 수정 후 재검사 |
| 55~60분 | 시연 리허설 | 시연 지원 | 재실행·전체 연결 확인 | 모델 동작 확인 |

## 6. 공통 연동 규격

시작 5분 안에 다음 필드와 판정값을 합의하고, 변경이 필요하면 관련 담당자에게 먼저 공유한다.

### 검사 API

`POST /api/check`

요청 예시:

```json
{
  "text": "이 씨 발아가 잘되네."
}
```

성공 응답 예시 — 형식 설명용이며 실제 모델 판정이나 측정값이 아니다:

```json
{
  "decision": "ALLOW",
  "reason": "식물의 씨가 발아하는 상황을 설명하는 문장입니다.",
  "baseline_flagged": true,
  "elapsed_ms": 1200
}
```

| 필드 | 형식 | 생성 담당 | 의미 |
|---|---|---|---|
| `text` | 문자열 | 프론트 | 공백을 보존한 입력 원문 |
| `decision` | `ALLOW` / `BLOCK` / `REVIEW` | AI | 문맥에 따른 최종 판정 |
| `reason` | 문자열 | AI | 짧은 한국어 판단 이유 |
| `baseline_flagged` | 불리언 | 백엔드 | 비교용 단순 필터의 검출 여부 |
| `elapsed_ms` | 0 이상의 숫자 | 백엔드 | 실제 요청 처리 시간, 밀리초 |

### AI 함수

- 함수: `classify(text)`
- 입력: 공백을 보존한 원문 문자열.
- 성공 반환: `decision`, `reason`을 가진 객체.
- 실패: 백엔드가 검사 실패로 처리할 수 있는 오류 전달.
- 단순 필터 결과와 처리 시간은 모델에게 생성시키지 않고 백엔드가 계산한다.

### Ollama 연결

- 기본 로컬 호출 주소: `http://localhost:11434/api/generate`
- 실제 설치한 로컬 모델 이름을 사용한다.
- `stream: false`로 완성된 응답을 한 번에 받는다.
- `format`에 JSON Schema를 지정해 출력 형식을 제한한다.
- `/api/generate` 응답의 `response`에 담긴 생성 텍스트를 JSON으로 파싱하고 검증한다.
- 프론트 화면에 구현용 설정값을 추가하기보다 AI 담당과 백엔드가 설정을 공유한다.

## 7. 대표 테스트와 시연

다음 판정은 테스트의 기대 결과이며 실제 모델 응답과 비교해야 한다.

| 입력 | 기대 판정 | 검증 목적 |
|---|---|---|
| 오늘 날씨가 좋다. | `ALLOW` | 일반 정상 문장 |
| 이 씨 발아가 잘되네. | `ALLOW` | 공백 제거로 생긴 금칙어의 오탐 |
| 이 사건의 시발점은 무엇인가요? | `ALLOW` | 정상 단어 내부의 부분 문자열 |
| 씨발, 짜증 나네. | `BLOCK` | 실제 욕설 사용 |
| 씨 발, 짜증 나네. | `BLOCK` | 공백을 삽입한 우회 욕설 |
| ㅅㅂ | `REVIEW` | 문맥이 부족한 초성 표현 |

별도로 빈 입력, 300자 초과, 모델 연결 실패, 응답 시간 초과 시 화면과 API가 올바르게 처리하는지 확인한다.

## 8. 완료 체크리스트

- [ ] Ollama의 로컬 모델에서 실제 응답을 받는다.
- [ ] 입력 원문의 띄어쓰기가 AI에 전달될 때 보존된다.
- [ ] 단순 필터 결과와 무관하게 모든 유효 입력을 LLM으로 검사한다.
- [ ] 화면에서 허용·차단·확인 필요와 판단 이유를 표시한다.
- [ ] 단순 필터와 AI 결과를 나란히 비교할 수 있다.
- [ ] 차단·확인 필요·검사 실패를 서로 구분해 표시한다.
- [ ] 입력 오류와 모델 호출 실패를 처리한다.
- [ ] 테스트 결과와 실제 처리 시간을 기록한다.
- [ ] 실행 방법을 정리하고 프로그램 재실행을 확인한다.
- [ ] 임시 데이터와 실제 모델 결과를 구분하고, 시연은 실제 모델로 진행한다.

## 9. 참고 문서

- [Ollama Generate API](https://docs.ollama.com/api/generate)
- [Ollama 구조화 출력](https://docs.ollama.com/capabilities/structured-outputs)
